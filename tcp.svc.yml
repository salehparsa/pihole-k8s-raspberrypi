apiVersion: v1
kind: Service
metadata:
  name: pihole-service-tcp
  namespace: pihole
spec:
  selector:
    app: pihole
  type: NodePort
  ports:
  - name: web
    nodePort: 30080
    port: 80
    protocol: TCP